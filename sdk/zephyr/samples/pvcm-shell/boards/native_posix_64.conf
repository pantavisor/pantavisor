# Override for native_posix: use UART transport instead of RPMsg
# (native_posix has no OpenAMP/RPMsg support)
CONFIG_PANTAVISOR_TRANSPORT_RPMSG=n
CONFIG_PANTAVISOR_TRANSPORT_UART=y
CONFIG_OPENAMP=n
CONFIG_SERIAL=y
CONFIG_UART_NATIVE_POSIX=y
